## 动手实践

目标：验证“读取优先级 + 回退 + 持久化”。

### 步骤 1：准备 Global 文件

`GlobalPreference.xml`

```xml
<?xml version="1.0"?>
<GlobalPreference>
  <Item key="renderer" value="software"/>
  <Item key="bgm_volume" value="80"/>
  <Item key="user_language" value="zh_cn"/>
  <Custom key="logLevel" value="info"/>
</GlobalPreference>
```

### 步骤 2：准备 Individual 文件

`Kirikiroid2Preference.xml`

```xml
<?xml version="1.0"?>
<GlobalPreference>
  <Item key="renderer" value="opengl"/>
  <Item key="bgm_volume" value="35"/>
</GlobalPreference>
```

### 步骤 3：准备 Locale 文件

`locale/zh_cn.xml`

```xml
<?xml version="1.0"?>
<Locale>
  <Item id="msgbox_ok" text="确定"/>
  <Item id="msgbox_yes" text="是"/>
  <Item id="msgbox_no" text="否"/>
  <Item id="retry" text="重试"/>
  <Item id="cancel" text="取消"/>
</Locale>
```

### 步骤 4：读取验证代码

```cpp
auto *g = GlobalConfigManager::GetInstance();
auto *i = IndividualConfigManager::GetInstance();
auto *l = LocaleConfigManager::GetInstance();

if(!i->UsePreferenceAt(gameFolder)) {
    i->CreatePreferenceAt(gameFolder);
}

l->Initialize(TVPGetCurrentLanguage());

std::string renderer = i->GetValue<std::string>("renderer", "software");
int bgm = i->GetValue<int>("bgm_volume", 100);
std::string okText = l->GetText("msgbox_ok");
```

预期：

1. `renderer == opengl`。  
2. `bgm == 35`。  
3. `okText == 确定`。  

### 步骤 5：回退验证

删除 Individual 中 `bgm_volume` 后再次读取，应该回退到 Global 的 `80`。

### 步骤 6：保存验证

```cpp
i->SetValueInt("bgm_volume", 40);
i->SaveToFile();
```

重启后应仍然读取到 40。

## 对照项目源码

核心文件：

1. `cpp/core/environ/ConfigManager/GlobalConfigManager.h`
2. `cpp/core/environ/ConfigManager/GlobalConfigManager.cpp`
3. `cpp/core/environ/ConfigManager/IndividualConfigManager.h`
4. `cpp/core/environ/ConfigManager/IndividualConfigManager.cpp`
5. `cpp/core/environ/ConfigManager/LocaleConfigManager.h`
6. `cpp/core/environ/ConfigManager/LocaleConfigManager.cpp`
7. `cpp/core/environ/Application.h`
8. `cpp/core/environ/Application.cpp`
9. `cpp/core/environ/Platform.h`
10. `cpp/core/environ/win32/Platform.cpp`
11. `cpp/core/environ/linux/Platform.cpp`
12. `cpp/core/environ/android/AndroidUtils.cpp`

关键行段：

- `GlobalConfigManager.cpp` 39-85：读取流程。  
- `GlobalConfigManager.cpp` 87-121：写回流程。  
- `GlobalConfigManager.cpp` 132-170：GetValue 特化。  
- `IndividualConfigManager.cpp` 58-84：覆盖优先级。  
- `LocaleConfigManager.cpp` 10-20：语言文件回退。  
- `LocaleConfigManager.cpp` 36-61：初始化与加载。  
- `Application.cpp` 118-135：消息框本地化。  
- `Application.cpp` 302-307：启动文案读取。  

## 本节小结

1. KrKr2 的三级配置系统解决了“全局默认、游戏覆盖、文本本地化”三类需求。
2. `iSysConfigManager` 提供统一的 XML 读写、字符串存储与模板化访问入口。
3. `GlobalConfigManager` 管理全局默认值；`IndividualConfigManager` 管理每个游戏的覆盖值。
4. `LocaleConfigManager` 管理区域化文本，语言选择优先用户设置。
5. 配置读取链和回退链清晰，便于定位问题。
6. 持久化由脏标记驱动，减少无效写盘。
7. 平台差异通过 PAL 接口封装，不污染配置层逻辑。

## 练习题与答案（≥3题，答案用 <details> 折叠）

### 题目 1
已知 Global 中 `renderer=software`，Individual 中 `renderer=opengl`，调用 `IndividualConfigManager::GetValue<std::string>("renderer", "software")`，结果是什么？

<details>
<summary>查看答案</summary>

返回 `opengl`。因为 Individual 特化会先拿 Global 结果作为默认链，再查 Individual；命中时 Individual 覆盖 Global。

</details>

### 题目 2
为什么当前实现里 `value="true"` 可能被读成 false？应如何修复？

<details>
<summary>查看答案</summary>

`GetValue<bool>` 调用了 `GetValue<int>`，而 `GetValue<int>` 用 `atoi`，`atoi("true")` 结果是 0。修复方式：要么约定布尔只写 `0/1`，要么升级 bool 特化以支持 `true/false/yes/no/on/off`。

</details>

### 题目 3
`CreatePreferenceAt` 与 `UsePreferenceAt` 的区别是什么？

<details>
<summary>查看答案</summary>

`CreatePreferenceAt` 只设置路径上下文，不读取文件；`UsePreferenceAt` 会先检查文件存在，存在时设置路径并调用 `Initialize` 读取。

</details>

## 下一步
1. 回看 `./README.md`，把 M03 的 Application 生命周期、PAL、Cocos2d-x 桥接和配置系统串成完整链路。  
2. 实作增强：为 `GetValue<bool>` 增加 `true/1/yes` 兼容，并补一组 Global/Individual/Locale 读取优先级回归测试。
