## 动手实践

### 实验 1：检测当前平台特性

创建一个 `platform_info.cpp` 文件，编译并运行它在你的平台上：

```cpp
// platform_info.cpp
#include <cstdint>
#include <iostream>
#include <bit> // C++20

int main() {
    std::cout << "=== 平台信息检测 ===" << std::endl;

    // 1. 检测操作系统
    std::cout << "\n--- 操作系统 ---" << std::endl;
#ifdef _WIN32
    std::cout << "操作系统: Windows" << std::endl;
#elif defined(__linux__) && defined(__ANDROID__)
    std::cout << "操作系统: Android" << std::endl;
#elif defined(__linux__)
    std::cout << "操作系统: Linux" << std::endl;
#elif defined(__APPLE__)
    std::cout << "操作系统: macOS" << std::endl;
#else
    std::cout << "操作系统: 未知" << std::endl;
#endif

    // 2. 检测数据类型大小
    std::cout << "\n--- 数据类型大小 ---" << std::endl;
    std::cout << "char:      " << sizeof(char) << " bytes" << std::endl;
    std::cout << "short:     " << sizeof(short) << " bytes" << std::endl;
    std::cout << "int:       " << sizeof(int) << " bytes" << std::endl;
    std::cout << "long:      " << sizeof(long) << " bytes" << std::endl;
    std::cout << "long long: " << sizeof(long long) << " bytes" << std::endl;
    std::cout << "size_t:    " << sizeof(size_t) << " bytes" << std::endl;
    std::cout << "wchar_t:   " << sizeof(wchar_t) << " bytes" << std::endl;
    std::cout << "pointer:   " << sizeof(void*) << " bytes" << std::endl;

    // 3. 检测字节序
    std::cout << "\n--- 字节序 ---" << std::endl;
    if constexpr (std::endian::native == std::endian::little) {
        std::cout << "字节序: Little-Endian (小端)" << std::endl;
    } else if constexpr (std::endian::native == std::endian::big) {
        std::cout << "字节序: Big-Endian (大端)" << std::endl;
    }

    // 4. 验证字节序
    uint32_t testVal = 0x01020304;
    uint8_t* bytes = reinterpret_cast<uint8_t*>(&testVal);
    std::cout << "0x01020304 在内存中: ";
    for (int i = 0; i < 4; i++) {
        std::cout << "0x" << std::hex << static_cast<int>(bytes[i]) << " ";
    }
    std::cout << std::endl;
    // 小端输出: 0x4 0x3 0x2 0x1
    // 大端输出: 0x1 0x2 0x3 0x4

    return 0;
}
```

**编译并运行：**

```bash
# Windows (MSVC)
cl /std:c++20 /EHsc platform_info.cpp /Fe:platform_info.exe
platform_info.exe

# Linux / macOS
g++ -std=c++20 -o platform_info platform_info.cpp
./platform_info

# 使用 CMake（推荐）
# CMakeLists.txt:
# cmake_minimum_required(VERSION 3.28)
# project(platform_info)
# set(CMAKE_CXX_STANDARD 20)
# add_executable(platform_info platform_info.cpp)
```

### 实验 2：跨平台文件操作

```cpp
// cross_file.cpp
#include <filesystem>
#include <fstream>
#include <iostream>
#include <string>

namespace fs = std::filesystem;

int main() {
    // 1. 创建目录（跨平台安全）
    fs::path testDir = fs::temp_directory_path() / "kr2_test";
    fs::create_directories(testDir);
    std::cout << "测试目录: " << testDir.string() << std::endl;

    // 2. 写入文件
    fs::path filePath = testDir / "test_file.txt";
    {
        std::ofstream out(filePath);
        out << "Hello from cross-platform C++!" << std::endl;
        out << "路径分隔符: " << fs::path::preferred_separator << std::endl;
    }

    // 3. 读取文件
    {
        std::ifstream in(filePath);
        std::string line;
        while (std::getline(in, line)) {
            std::cout << "读取: " << line << std::endl;
        }
    }

    // 4. 遍历目录
    for (const auto& entry : fs::directory_iterator(testDir)) {
        std::cout << "文件: " << entry.path().filename()
                  << " 大小: " << entry.file_size() << " bytes" << std::endl;
    }

    // 5. 清理
    fs::remove_all(testDir);
    std::cout << "清理完成" << std::endl;

    return 0;
}
```

---

## 本节小结

- **文件系统**：路径分隔符（`\` vs `/`）、大小写敏感性、最大路径长度、文件锁定行为在各平台间差异巨大。使用 `std::filesystem` 是最安全的跨平台方案
- **线程模型**：`std::thread` 提供了跨平台基础，但线程优先级、Android JNI 线程绑定等仍需平台特定代码
- **字节序**：KrKr2 的四个目标平台都是小端序，但处理二进制文件格式时仍应使用显式字节序读写函数
- **数据类型**：`long` 和 `wchar_t` 是最危险的跨平台类型，应使用 `<cstdint>` 中的固定宽度类型
- **字符编码**：Windows 默认非 UTF-8，使用 Wide API 和 UTF-8/UTF-16 转换；Unix-like 平台默认 UTF-8

---

## 练习题与答案

### 题目 1：平台特征判断

以下代码在 Windows x64 (MSVC) 和 Linux x64 (GCC) 上编译运行，分别输出什么？

```cpp
#include <iostream>
int main() {
    std::cout << sizeof(long) << " " << sizeof(wchar_t) << std::endl;
    return 0;
}
```

<details>
<summary>查看答案</summary>

- **Windows x64 (MSVC)**：输出 `4 2`
  - `long` 在 MSVC 中始终是 4 字节（ILP32LL 数据模型）
  - `wchar_t` 在 Windows 上是 2 字节（UTF-16）
- **Linux x64 (GCC)**：输出 `8 4`
  - `long` 在 64 位 Linux 中是 8 字节（LP64 数据模型）
  - `wchar_t` 在 Linux 上是 4 字节（UTF-32）

**教训：** 永远不要用 `long` 来存储需要跨平台传输的数据，使用 `int32_t` 或 `int64_t`。

</details>

### 题目 2：字节序转换

编写一个函数 `readLE16`，从 `uint8_t*` 指针读取一个小端序的 16 位无符号整数。要求在任何字节序的平台上都能正确工作。

<details>
<summary>查看答案</summary>

```cpp
#include <cstdint>

uint16_t readLE16(const uint8_t* data) {
    // 低字节在低地址（小端），手动按位组装
    return static_cast<uint16_t>(data[0])
         | (static_cast<uint16_t>(data[1]) << 8);
}

// 测试
#include <iostream>
int main() {
    uint8_t data[] = {0x34, 0x12}; // 小端表示的 0x1234
    uint16_t value = readLE16(data);
    std::cout << std::hex << "0x" << value << std::endl; // 输出: 0x1234
    return 0;
}
```

**关键点：**
- 不使用 `reinterpret_cast`（避免未定义行为和对齐问题）
- 显式按字节组装，不依赖平台字节序
- 在大端和小端平台上都能得到正确结果

</details>

### 题目 3：跨平台路径处理

以下代码在 Linux 上运行时，有什么问题？如何修复？

```cpp
#include <fstream>
std::ifstream file("C:\\Data\\Scripts\\startup.tjs");
if (file.is_open()) {
    // 处理文件
}
```

<details>
<summary>查看答案</summary>

**问题：**
1. `C:\Data\Scripts\startup.tjs` 是 Windows 绝对路径，Linux 上不存在 `C:` 盘符
2. 即使路径存在，`\D`、`\S`、`\s` 在字符串中可能被解释为转义字符（虽然这几个不是有效转义，编译器会警告）
3. Linux 文件系统区分大小写，`startup.tjs` 和 `Startup.TJS` 是不同文件

**修复方案：**

```cpp
#include <filesystem>
#include <fstream>

namespace fs = std::filesystem;

// 1. 使用相对路径，避免平台绝对路径
fs::path scriptPath = fs::path("Data") / "Scripts" / "startup.tjs";

// 2. 如果文件名可能大小写不匹配，实现不敏感查找
std::ifstream openCaseInsensitive(const fs::path& dir, const std::string& filename) {
    std::string lowerTarget;
    for (char c : filename) lowerTarget += std::tolower(c);

    for (const auto& entry : fs::directory_iterator(dir)) {
        std::string entryName = entry.path().filename().string();
        std::string lowerEntry;
        for (char c : entryName) lowerEntry += std::tolower(c);

        if (lowerEntry == lowerTarget) {
            return std::ifstream(entry.path());
        }
    }
    return std::ifstream(); // 未找到，返回无效流
}

int main() {
    auto file = openCaseInsensitive("Data/Scripts", "startup.tjs");
    if (file.is_open()) {
        // 处理文件 — 即使实际文件名是 Startup.TJS 也能找到
    }
    return 0;
}
```

</details>

---

## 下一步

下一节 [预处理器与条件编译](../02-预处理器与条件编译/) 将深入讲解 C++ 预处理器的跨平台用法：`#ifdef`/`#if defined` 的区别、各平台预定义宏一览、以及如何通过 CMake 向代码传递平台信息。
